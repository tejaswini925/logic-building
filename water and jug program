#include <stdio.h>
int main()
int gcd(int a, int b) {
    if (b == 0) return a;
    return gcd(b, a % b);
}
int canMeasureWater(int x, int y, int z) {
    if (z == 0) return 1;
    if (x + y < z) return 0;
    if (z % gcd(x, y) == 0)
        return 1;
    else
        return 0;
}
