class Solution:
    def majorityElement(self, nums):
        count = 0

        for x in nums:
            if count == 0:
                ans = x
            if x == ans:
                count += 1
            else:
                count -= 1

        return ans
