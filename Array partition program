class Solution:
    def arrayPairSum(self, nums): 
        nums.sort()
        return sum(nums[::2])
        result = 0
        for i in range(0, len(nums), 2):
            result += nums[i]
            return result
