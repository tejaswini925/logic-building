graph = {
    0: [1, 2],
    1: [0, 3],
    2: [0],
    3: [1]
}

visited = set()

def dfs(node):
    if node in visited:
        return
    
    print(node, end=" ")
    visited.add(node)
    
    for neighbor in graph[node]:
        dfs(neighbor)

dfs(0)
