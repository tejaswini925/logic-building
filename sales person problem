#include <stdio.h>
#include <string.h>
int main() {
    char name[][10] = {"John","Amy","Mark"};
    int soldToRed[] = {1,0,0};  // 1 = sold to RED
    for(int i=0;i<3;i++)
        if(!soldToRed[i])
            printf("%s\n", name[i]);
    return 0;
}
