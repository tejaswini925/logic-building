#include<stdio.h>
void main()
{
int a[50],i,j,n,temp;
printf("enter a number of elements\n");
scanf("%d",&n);
printf("enter elements\n");
for(i=0;i<n;i++)
{
for(j=i+1;j<=n-i;j++)
{
if(a[i]>=a[j])
temp=a[i];
a[i]=a[j+1];
a[j+1]=temp;
}}}

