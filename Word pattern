class Solution:
    def wordPattern(self, pattern, s):
        a = pattern
        b = s.split()

        if len(a) != len(b):
            return False

        return len(set(zip(a, b))) == len(set(a)) == len(set(b))
